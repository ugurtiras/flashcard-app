# Production Environment Variables
# Copy this to .env.prod and fill in your actual values

# Docker Hub credentials
DOCKERHUB_USERNAME=your_dockerhub_username

# Database (AWS RDS PostgreSQL)
DATABASE_URL=postgresql://db_user:db_password@your-rds-endpoint.rds.amazonaws.com:5432/flashcard_prod

# Security
JWT_SECRET=your_secure_jwt_secret_here_min_32_chars

# Frontend API URL (EC2 instance IP or domain)
VITE_API_BASE_URL=http://your-ec2-public-ip:5000
# or if using domain:
# VITE_API_BASE_URL=https://api.yourdomain.com
